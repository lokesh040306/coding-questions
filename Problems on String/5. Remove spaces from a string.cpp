/*

class Solution {
public:
    string removeWhitespaces(string str) {
    
        string result = "";

        for(char ch : str) {
        
            if(ch != ' ' && ch != '\t' && ch != '\n') {
                result += c;
            }
        }

        return result;
    }
};

*/