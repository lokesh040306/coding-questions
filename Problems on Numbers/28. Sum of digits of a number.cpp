/*

class Solution {
public:
    int addDigits(int num) {
    
        if (num == 0) {
            return 0;
        }

        // Use digital root formula: 1 + (num - 1) % 9
        return 1 + (num - 1) % 9;
    }
};

*/