/*

void getMedian(int arr[], int n) {
    sort(arr, arr + n);  // Sort the array to arrange elements in order

    if (n % 2 == 0) {
    
        // If the array size is even, calculate the average of two middle elements
        int ind1 = (n / 2) - 1;
        int ind2 = (n / 2);
        cout << (double)(arr[ind1] + arr[ind2]) / 2;  
    } 
    else {
        // If the array size is odd, return the middle element
        cout << arr[(n / 2)];
    }
}

*/